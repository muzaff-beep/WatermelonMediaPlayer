[package]
name = "watermelon-core"
version = "0.1.0"
edition = "2021"

[dependencies]
ffmpeg-next = { version = "6.0", features = ["static", "build-from-source", "codec", "format", "software-resampling", "software-scaling"] }